.env
CACHE
.idea/

nole/__pycache__/
staticfiles/
*.pyc
*.pdf

node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
package-lock.json
yarn.lock

# Python virtual environments
.venv/
venv/

# Local SQLite databases
*.sqlite3

# User-uploaded media. At runtime these live on the Upsun `media` mount (or S3),
# never in git. Keep the tracked default avatar; ignore all other uploads.
/media/*
!/media/avatars/
/media/avatars/*
!/media/avatars/default.png